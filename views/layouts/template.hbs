<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="style.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-3">
        <div class="container bg-primary text-white p-3">
            <h1>Welcome to News Search!</h1>
        </div>
        <div class="container">
            <form action="/results" method="GET">
                <div class="form-group row mt-5 ml-5 mr-5">
                    <label for="search" class="mt-3">Search</label>
                    <input type="text" name="search" class="form-control" placeholder="Enter search terms for news." required="required">
                </div>
                <div class="form-group row ml-5 btn-group btn-group-toggle" data-toggle="buttons">
                    {{#each images}}
                            <div class="form-check col-4 mt-5">
                                <label for="{{this}}" class="btn btn-outline-primary">
                                    <img src="/images/{{this}}.png" alt="">
                                </label>
                                <input class="ml-3 mt-3 form-check-input" type="radio" name="country" id="{{this}}" value="{{this}}" required="required">
                            </div>
                    {{/each}}
                </div>
                <div class="container bg-primary text-white">
                    <div class="form-group p-3 ">
                        <div class="selectgrp justify-content-start">
                            <select size="7" class="form-control" name="category" id="category" required="required">
                                {{#each categories}}
                                    <option value="{{this}}">{{this}}</option>
                                {{/each}}
                            </select>
                        </div>
                    </div>
                </div>
                <div class="form-group">
                    <div class="d-flex justify-content-end mt-3">
                        <button type="submit" class="btn-outline-primary btn btn-lg">Search for News.</button>
                    </div>
                </div>
            </form>
        </div>
        

        {{{body}}}
    </div>
</body>
</html>